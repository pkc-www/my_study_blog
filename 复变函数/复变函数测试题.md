### 复变函数测试题

**一、填空题 (3' × 8)**

1. 级数 $\sum_{n=1}^{\infty} n(n-1)z^n$ 的和函数为：________________。
2. 级数 $\sum_{n=0}^{\infty} 3^{-n^2} z^n$ 的收敛圆环域为：________________。
3. 将 $e^{\frac{1}{1-z}}$ 在 $|z|>1$ 内展开为 Laurent 级数，其中 $\frac{1}{z}$ 的系数为：________________。
4. 函数 $f(z) = \frac{1}{\sin z} - \frac{1}{z} + \cos \frac{1-z}{z}$，则 $z_0=0$ 是其________________（填奇点类型）。
5. 级数 $\sum_{n=1}^{\infty} \frac{z^{2n}}{\sqrt{n} \cdot 5^n}$ 在 $z = 1+2i$ 处是________________（填发散或具体收敛类型）。
6. 级数 $\sum_{n=1}^{\infty} \frac{n^{2n}}{(2n)!} z^n$ 的收敛半径为：________________。
7. $i^{\sqrt{3}}$ 的多值表达式为：________________。
8. 计算留数：$\text{Res} \left[ \frac{1}{z^4 \sin(\frac{1}{z})} + \sin(\frac{z}{z-1}), \infty \right] =$ ________________。

**二、计算积分**
$$\oint_{|z|=3} \frac{z^2 e^{\frac{1}{z-1}}}{(z-1)(z-2)} dz$$

**三、计算积分**
$$\int_{0}^{2\pi} \frac{\cos(2019\theta)}{1+p^2-2p\cos\theta} d\theta$$

**四、计算积分**
$$\int_{-\infty}^{+\infty} \frac{\sin x}{x^2-4x+8} dx$$

**五、求解析函数**
设整函数 $f(z) = u + iv$ （其中 $z = x + iy$），其已知实部与虚部满足关系：
$$u(x, y) + v(x, y) = (x-y)(x^2 + 4xy + y^2) - 2(x + y)$$
且满足 $f(0) = 0$，求 $f(z)$ 的具体表达式。

**六、分析题**
已知函数 $f(z)$ 在单位圆内 $D = \{z : |z| < 1\}$ 上解析，且满足 $f(\frac{1}{n}) = \frac{n-1}{n^2} \quad (n \ge 2)$。
1. 是否存在 $D$ 上的解析函数满足上述条件？
2. 若存在，请举出一例；若不存在，请说明理由。



**(ygw版) 2018-2019 复变期末 (秋)**

#### **一、 填空题 (3' × 9 = 27')**
*(填空顺序不保证，每题3分)*

1. $\lim_{n \to \infty} n \ln(1 + \sin \frac{i\pi}{n}) = \underline{\qquad}$
2. $f(z) = z^{2019} + 2018z$，设 $M = \max \{ |f(z)| \mid |z| \le 1 \}$，则 $M = \underline{\qquad}$
3. $\sum_{n=1}^{+\infty} nz^n$ 在收敛圆盘内的和函数为 $\underline{\qquad}$
4. $\sum_{n=1}^{+\infty} \frac{\sin \frac{i}{n}}{n} z^n$ 在 -1 处的敛散性为 $\underline{\qquad}$（填“发散”或具体收敛类型）
5. $\sum_{n=-\infty}^{+\infty} 3^{-|n|} z^n$ 的收敛圆环为 $\underline{\qquad}$
6. $\text{Res} \left[ z \sin \frac{z}{z-1}, \infty \right] = \underline{\qquad}$
7. 实函数 $u(x,y) = e^{py} \sin x$ 是 $\mathbb{R}^2$ 上的调和函数，则实系数 $p = \underline{\qquad}$
8. $e^{\frac{1}{z-1}}$ 在 $|z| > 1$ 的洛朗展开式中，$\frac{1}{z^2}$ 的系数 $C_{-2} = \underline{\qquad}$
9. $\sum_{n=1}^{+\infty} \frac{n^n}{n!} z^{3n}$ 的收敛半径为 $\underline{\qquad}$

---

#### **解答题**
*(二到四的分值不确定，但总和为 30')*

**二、(10')** 求 $I_1 = \frac{1}{2\pi i} \oint_{|z|=2} \frac{z \sin \frac{1}{z}}{z-1} dz$

**三、(10')** 用留数的方法求 $\int_0^{+\infty} \frac{\cos x}{(x^2+1)^2} dx$

**四、(10')** 用留数的方法求 $\int_0^{2\pi} \frac{dx}{p + \sin x}$，其中 $p > 1$，$p$ 为实数

**五、(12')** 设 $f(z) = \frac{1}{z \sin \frac{1}{z}} + \frac{\cos \frac{\pi z}{2}}{z^2(z-1)^4}$，求在扩充复平面 $\overline{\mathbb{C}}$ 上的所有奇点并分类（要说明理由，若是极点还需说明是几级极点）。

**六、(6')** 判断命题是否正确，并证明。
**命题：** $\exists$ 正常数 $M, \forall z \in \mathbb{C}, \left| \sum_{n=1}^\infty \frac{e^{iz^n}}{n^n} \right| < M$。